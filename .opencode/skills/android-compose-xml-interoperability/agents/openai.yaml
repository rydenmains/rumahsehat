interface:
  display_name: "Android Compose XML Interoperability"
  short_description: "Bridge Compose and the View system safely during incremental migrations, interoperability screens, and shared theming"
  default_prompt: "Use $android-compose-xml-interoperability when the request is about bridge Compose and the View system safely during incremental migrations, interoperability screens, and shared theming."
