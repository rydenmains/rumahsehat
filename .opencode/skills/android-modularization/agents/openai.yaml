interface:
  display_name: "Android Modularization"
  short_description: "Design Android repositories with feature, core, and build-logic modules that scale without cyclic dependencies"
  default_prompt: "Use $android-modularization when the request is about design Android repositories with feature, core, and build-logic modules that scale without cyclic dependencies."
